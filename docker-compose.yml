services:
  test-db:
    image: postgres:latest
    container_name: ecommerce-db
    restart: always
    env_file:
      - .env
    ports:
      - "5457:5432"
    volumes:
      - ./data/test/postgres_data:/var/lib/postgresql/data  # Fixed path